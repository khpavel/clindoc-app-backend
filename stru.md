# Структура проекта и подход к разработке

## Общее описание проекта

**CSR Assistant Backend** — это backend API для приложения CSR Assistant, построенный на FastAPI. Проект предназначен для управления клиническими исследованиями (studies), создания и редактирования документов CSR (Clinical Study Report), работы с исходными документами и интеграции с AI для генерации текста разделов.

---

## Структура каталогов

### Корневой каталог проекта

```
clindoc-app-backend/
├── app/                    # Основное приложение
├── alembic/               # Миграции базы данных
├── storage/               # Хранилище загруженных файлов
├── venv/                  # Виртуальное окружение Python (не коммитится)
├── alembic.ini            # Конфигурация Alembic
├── requirements.txt       # Зависимости Python
├── README.md              # Документация проекта
├── API_SPECIFICATION.md   # Спецификация API
├── run_server.bat         # Скрипт запуска сервера (Windows CMD)
├── run_server.ps1          # Скрипт запуска сервера (Windows PowerShell)
├── reinstall_venv.bat     # Скрипт переустановки venv (Windows CMD)
└── reinstall_venv.ps1     # Скрипт переустановки venv (Windows PowerShell)
```

---

## Детальное описание каталогов и файлов

### 📁 `app/` — Основное приложение

Главный каталог приложения, содержащий всю бизнес-логику, API endpoints, модели данных и сервисы.

#### 📄 `app/main.py`
**Назначение:** Точка входа приложения FastAPI.

**Основные функции:**
- Инициализация FastAPI приложения
- Настройка CORS middleware для работы с фронтендом
- Регистрация всех API роутеров (auth, studies, csr, ai, sources, templates)
- Глобальный обработчик исключений
- Создание таблиц базы данных при старте (для MVP)
- Настройка логирования
- Health check endpoint (`/health`)

**Ключевые компоненты:**
- Подключение всех моделей SQLAlchemy для их регистрации
- Настройка CORS для `localhost:5173` (Vite dev server)
- Интеграция всех роутеров с префиксом `/api/v1`

---

#### 📁 `app/api/` — API Endpoints

Каталог содержит все HTTP endpoints приложения, организованные по версиям API.

##### 📁 `app/api/v1/` — API версии 1

Содержит роутеры для всех endpoints версии 1 API.

###### 📄 `app/api/v1/auth.py`
**Назначение:** Endpoints для аутентификации пользователей.

**Endpoints:**
- `POST /api/v1/auth/register` — регистрация нового пользователя
- `POST /api/v1/auth/token` — получение JWT токена (OAuth2 password flow)

**Функциональность:**
- Проверка уникальности username и email
- Хеширование паролей с помощью bcrypt
- Генерация JWT токенов
- Валидация данных через Pydantic схемы

###### 📄 `app/api/v1/studies.py`
**Назначение:** Endpoints для управления клиническими исследованиями.

**Endpoints:**
- `POST /api/v1/studies` — создание нового исследования
- `GET /api/v1/studies` — получение списка всех исследований

**Функциональность:**
- CRUD операции для исследований
- Проверка уникальности кода исследования
- Требует аутентификации (через `get_current_active_user`)

###### 📄 `app/api/v1/csr.py`
**Назначение:** Endpoints для работы с документами CSR (Clinical Study Report).

**Endpoints:**
- `GET /api/v1/csr/{study_id}` — получение CSR документа для исследования
- `GET /api/v1/csr/{study_id}/sections` — получение всех разделов CSR
- `GET /api/v1/csr/sections/{section_id}/versions/latest` — получение последней версии раздела
- `POST /api/v1/csr/sections/{section_id}/versions` — создание новой версии раздела

**Функциональность:**
- Автоматическое создание CSR с дефолтными разделами при первом обращении
- Версионирование разделов CSR
- Управление порядком разделов через `order_index`

###### 📄 `app/api/v1/ai.py`
**Назначение:** Endpoints для интеграции с AI/LLM для генерации текста.

**Endpoints:**
- `POST /api/v1/ai/generate-section-text` — генерация текста для раздела CSR

**Функциональность:**
- Заглушка для будущей интеграции с LLM
- Принимает `section_id` и `prompt` для генерации текста
- Возвращает сгенерированный текст и информацию о модели

###### 📄 `app/api/v1/sources.py`
**Назначение:** Endpoints для загрузки и управления исходными документами.

**Endpoints:**
- `POST /api/v1/sources/{study_id}/upload` — загрузка исходного документа
- `GET /api/v1/sources/{study_id}` — получение списка документов исследования

**Функциональность:**
- Загрузка файлов через `multipart/form-data`
- Сохранение файлов в `storage/study_{study_id}/`
- Хранение метаданных в базе данных
- Поддержка различных типов документов

###### 📄 `app/api/v1/templates.py`
**Назначение:** Endpoints для работы с шаблонами документов.

**Endpoints:**
- (Детали зависят от реализации)

**Функциональность:**
- Управление шаблонами для генерации документов
- Рендеринг шаблонов с подстановкой данных

---

#### 📁 `app/core/` — Основные настройки и утилиты

Каталог содержит конфигурацию приложения и базовые утилиты.

###### 📄 `app/core/config.py`
**Назначение:** Управление конфигурацией приложения через переменные окружения.

**Функциональность:**
- Загрузка настроек из `.env.dev` или `.env.prod` в зависимости от `APP_ENV`
- Настройки базы данных (`database_url`)
- Настройки JWT аутентификации (`secret_key`, `algorithm`, `access_token_expire_minutes`)
- Настройки хранилища файлов (`storage_dir`)
- Валидация обязательных параметров при старте

**Особенности:**
- Использует `pydantic-settings` для валидации
- Поддержка разных окружений (dev/prod)
- Автоматическое определение файла конфигурации по переменной `APP_ENV`

###### 📄 `app/core/security.py`
**Назначение:** Утилиты для безопасности и аутентификации.

**Функциональность:**
- `get_password_hash()` — хеширование паролей с помощью bcrypt
- `verify_password()` — проверка паролей
- `create_access_token()` — создание JWT токенов

**Особенности:**
- Обработка ограничения bcrypt на длину пароля (72 байта)
- Использование `python-jose` для работы с JWT
- Настройка времени жизни токенов через конфигурацию

###### 📄 `app/core/storage.py`
**Назначение:** Утилиты для работы с файловым хранилищем.

**Функциональность:**
- `get_storage_dir()` — получение базовой директории хранилища
- `build_study_source_path()` — построение пути для файла исследования

**Особенности:**
- Автоматическое создание директорий при необходимости
- Организация файлов по исследованиям (`study_{study_id}/`)

---

#### 📁 `app/db/` — Работа с базой данных

Каталог содержит настройки подключения к базе данных и сессий SQLAlchemy.

###### 📄 `app/db/session.py`
**Назначение:** Настройка подключения к базе данных и управление сессиями.

**Функциональность:**
- Создание engine SQLAlchemy
- Настройка `SessionLocal` для создания сессий
- `get_db()` — dependency для FastAPI, предоставляющая сессию БД
- Базовый класс `Base` для моделей SQLAlchemy

**Особенности:**
- Использует connection pooling
- Автоматическое закрытие сессий после использования
- Интеграция с настройками из `app/core/config.py`

---

#### 📁 `app/deps/` — Dependencies для FastAPI

Каталог содержит зависимости (dependencies) для использования в endpoints.

###### 📄 `app/deps/auth.py`
**Назначение:** Dependencies для аутентификации и авторизации.

**Функциональность:**
- `get_current_user()` — получение текущего пользователя из JWT токена
- `get_current_active_user()` — получение активного пользователя (проверка `is_active`)

**Особенности:**
- Использует OAuth2PasswordBearer для извлечения токена из заголовка
- Валидация JWT токена
- Проверка существования пользователя в БД
- Обработка ошибок аутентификации

---

#### 📁 `app/models/` — SQLAlchemy модели

Каталог содержит модели данных для работы с базой данных.

###### 📄 `app/models/user.py`
**Назначение:** Модель пользователя.

**Поля:**
- `id` — первичный ключ
- `username` — уникальное имя пользователя
- `full_name` — полное имя
- `email` — email (опционально)
- `hashed_password` — хешированный пароль
- `is_active` — флаг активности пользователя
- `created_at` — дата создания

###### 📄 `app/models/study.py`
**Назначение:** Модель клинического исследования.

**Поля:**
- `id` — первичный ключ
- `code` — уникальный код исследования
- `title` — название исследования
- `phase` — фаза исследования (опционально)

###### 📄 `app/models/csr.py`
**Назначение:** Модели для CSR документов и их разделов.

**Модели:**
- `CSRDocument` — документ CSR, связанный с исследованием
- `CSRSection` — раздел CSR документа
- `CSRSectionVersion` — версия раздела с текстом и метаданными

**Связи:**
- CSRDocument → Study (один к одному)
- CSRSection → CSRDocument (многие к одному)
- CSRSectionVersion → CSRSection (многие к одному)

###### 📄 `app/models/source.py`
**Назначение:** Модель исходного документа.

**Поля:**
- `id` — первичный ключ
- `study_id` — ID исследования
- `type` — тип документа
- `file_name` — имя файла
- `uploaded_at` — дата загрузки
- `uploaded_by` — пользователь, загрузивший файл

###### 📄 `app/models/template.py`
**Назначение:** Модель шаблона документа.

**Поля:**
- (Детали зависят от реализации)

---

#### 📁 `app/schemas/` — Pydantic схемы

Каталог содержит Pydantic схемы для валидации входных и выходных данных API.

###### 📄 `app/schemas/user.py`
**Назначение:** Схемы для работы с пользователями.

**Схемы:**
- `UserCreate` — данные для создания пользователя
- `UserRead` — данные пользователя для ответа API
- `Token` — JWT токен и его тип
- `TokenData` — данные из JWT токена

###### 📄 `app/schemas/study.py`
**Назначение:** Схемы для работы с исследованиями.

**Схемы:**
- `StudyCreate` — данные для создания исследования
- `StudyRead` — данные исследования для ответа API

###### 📄 `app/schemas/csr.py`
**Назначение:** Схемы для работы с CSR документами.

**Схемы:**
- `CSRDocumentRead` — данные CSR документа
- `CSRSectionRead` — данные раздела CSR
- `CSRSectionVersionRead` — данные версии раздела
- `CSRSectionVersionCreate` — данные для создания версии

###### 📄 `app/schemas/source.py`
**Назначение:** Схемы для работы с исходными документами.

**Схемы:**
- `SourceCreate` — данные для загрузки документа
- `SourceRead` — данные документа для ответа API

###### 📄 `app/schemas/ai.py`
**Назначение:** Схемы для работы с AI endpoints.

**Схемы:**
- `GenerateSectionTextRequest` — запрос на генерацию текста
- `GenerateSectionTextResponse` — ответ с сгенерированным текстом

###### 📄 `app/schemas/template.py`
**Назначение:** Схемы для работы с шаблонами.

**Схемы:**
- (Детали зависят от реализации)

---

#### 📁 `app/services/` — Бизнес-логика и сервисы

Каталог содержит бизнес-логику, вынесенную из endpoints для переиспользования.

###### 📄 `app/services/csr_defaults.py`
**Назначение:** Сервис для создания CSR документов с дефолтными разделами.

**Функциональность:**
- Создание CSR документа с предопределенными разделами
- Настройка порядка разделов через `order_index`
- Инициализация структуры CSR при первом обращении

###### 📄 `app/services/template_renderer.py`
**Назначение:** Сервис для рендеринга шаблонов документов.

**Функциональность:**
- Подстановка данных в шаблоны
- Генерация финальных документов из шаблонов

---

### 📁 `alembic/` — Миграции базы данных

Каталог содержит конфигурацию и скрипты миграций базы данных Alembic.

#### 📄 `alembic/env.py`
**Назначение:** Конфигурация окружения Alembic для выполнения миграций.

**Функциональность:**
- Настройка подключения к БД из `app.core.config`
- Регистрация всех моделей для автогенерации миграций
- Поддержка online и offline режимов миграций

#### 📄 `alembic/script.py.mako`
**Назначение:** Шаблон для генерации новых миграций.

#### 📄 `alembic/__init__.py`
**Назначение:** Инициализация пакета Alembic.

#### 📁 `alembic/versions/`
**Назначение:** Каталог для хранения файлов миграций.

**Содержимое:**
- Файлы миграций в формате `{revision}_{description}.py`
- Каждая миграция содержит изменения схемы БД (создание/изменение таблиц, колонок и т.д.)

---

### 📁 `storage/` — Файловое хранилище

**Назначение:** Хранилище загруженных исходных документов.

**Структура:**
```
storage/
└── study_{study_id}/
    ├── file1.pdf
    ├── file2.pdf
    └── ...
```

**Особенности:**
- Файлы организованы по исследованиям
- Каждое исследование имеет свою директорию
- Файлы сохраняются с оригинальными именами

---

### 📁 `venv/` — Виртуальное окружение

**Назначение:** Виртуальное окружение Python с установленными зависимостями.

**Примечание:** Этот каталог не должен коммититься в Git (должен быть в `.gitignore`).

---

## Файлы в корне проекта

### 📄 `alembic.ini`
**Назначение:** Конфигурационный файл Alembic для управления миграциями.

**Содержит:**
- Путь к скриптам миграций
- Настройки логирования
- Шаблоны для генерации миграций
- Конфигурация подключения к БД (переопределяется в `env.py`)

### 📄 `requirements.txt`
**Назначение:** Список зависимостей Python проекта.

**Основные зависимости:**
- `fastapi` — веб-фреймворк
- `uvicorn[standard]` — ASGI сервер
- `sqlalchemy` — ORM для работы с БД
- `psycopg2-binary` — драйвер PostgreSQL
- `pydantic-settings` — настройки из переменных окружения
- `python-dotenv` — загрузка `.env` файлов
- `alembic` — миграции БД
- `python-jose[cryptography]` — работа с JWT
- `bcrypt` — хеширование паролей
- `python-multipart` — обработка multipart/form-data
- `pytest`, `httpx` — тестирование

### 📄 `README.md`
**Назначение:** Основная документация проекта с инструкциями по установке и запуску.

### 📄 `API_SPECIFICATION.md`
**Назначение:** Детальная спецификация всех API endpoints с примерами запросов и ответов.

### 📄 `run_server.bat` / `run_server.ps1`
**Назначение:** Скрипты для запуска сервера разработки.

**Функциональность:**
- Активация виртуального окружения
- Запуск uvicorn с флагом `--reload` для автоматической перезагрузки

### 📄 `reinstall_venv.bat` / `reinstall_venv.ps1`
**Назначение:** Скрипты для переустановки виртуального окружения.

**Функциональность:**
- Удаление старого venv
- Создание нового venv
- Установка зависимостей из `requirements.txt`

---

## Подход к разработке

### Архитектурный подход

Проект использует **многослойную архитектуру (Layered Architecture)** с четким разделением ответственности:

1. **Слой представления (Presentation Layer)** — `app/api/v1/`
   - Обработка HTTP запросов и ответов
   - Валидация входных данных через Pydantic схемы
   - Минимальная бизнес-логика

2. **Слой бизнес-логики (Business Logic Layer)** — `app/services/`
   - Вынесенная бизнес-логика
   - Переиспользуемые сервисы
   - Обработка сложных операций

3. **Слой доступа к данным (Data Access Layer)** — `app/models/`, `app/db/`
   - SQLAlchemy модели
   - Управление сессиями БД
   - ORM маппинг

4. **Слой инфраструктуры (Infrastructure Layer)** — `app/core/`
   - Конфигурация
   - Утилиты безопасности
   - Файловое хранилище

### Принципы проектирования

#### 1. **Separation of Concerns (Разделение ответственности)**
- Каждый модуль отвечает за свою область
- API endpoints не содержат бизнес-логику
- Модели БД отделены от схем валидации

#### 2. **Dependency Injection (Внедрение зависимостей)**
- Использование FastAPI Depends для внедрения зависимостей
- `get_db()` для сессий БД
- `get_current_user()` для аутентификации
- Легкое тестирование через моки

#### 3. **Versioning API**
- API организовано по версиям (`/api/v1/`)
- Возможность добавления новых версий без breaking changes

#### 4. **Configuration Management**
- Настройки через переменные окружения
- Поддержка разных окружений (dev/prod)
- Валидация конфигурации при старте

#### 5. **Database Migrations**
- Использование Alembic для управления схемой БД
- Версионирование изменений БД
- Автогенерация миграций из моделей

### Паттерны проектирования

#### 1. **Repository Pattern (частично)**
- Модели SQLAlchemy используются напрямую в endpoints
- Для сложных случаев можно добавить репозитории

#### 2. **Service Layer Pattern**
- Бизнес-логика вынесена в `app/services/`
- Пример: `csr_defaults.py` для создания дефолтных CSR

#### 3. **Dependency Injection Pattern**
- Широкое использование FastAPI Depends
- Легкая замена зависимостей для тестирования

#### 4. **Factory Pattern**
- `SessionLocal` для создания сессий БД
- `get_storage_dir()` для создания путей к файлам

### Технологический стек

- **FastAPI** — современный, быстрый веб-фреймворк с автоматической документацией
- **SQLAlchemy** — мощный ORM для работы с БД
- **PostgreSQL** — реляционная БД
- **Pydantic** — валидация данных и настройки
- **Alembic** — миграции БД
- **JWT (python-jose)** — аутентификация через токены
- **bcrypt** — безопасное хеширование паролей

### Подход к безопасности

1. **Аутентификация:**
   - JWT токены с настраиваемым временем жизни
   - OAuth2 password flow
   - Хеширование паролей с помощью bcrypt

2. **Авторизация:**
   - Проверка активности пользователя
   - Dependency для проверки прав доступа

3. **Валидация данных:**
   - Pydantic схемы для валидации входных данных
   - Защита от SQL injection через SQLAlchemy
   - Проверка типов на уровне API

### Подход к разработке

1. **Итеративная разработка:**
   - MVP подход с базовой функциональностью
   - Постепенное добавление функций

2. **Автоматическая документация:**
   - Swagger UI (`/docs`)
   - ReDoc (`/redoc`)
   - OpenAPI схема (`/openapi.json`)

3. **Логирование:**
   - Настроенное логирование через стандартный модуль Python
   - Глобальный обработчик исключений с логированием

4. **CORS:**
   - Настроен для работы с фронтендом на `localhost:5173`
   - Гибкая настройка для разных окружений

### Управление версиями и миграциями

- **Alembic** для управления схемой БД
- Автогенерация миграций из моделей
- Версионирование изменений БД
- Поддержка отката миграций

### Тестирование

- Использование `pytest` для unit и integration тестов
- `httpx` для тестирования API endpoints
- Зависимости настроены для легкого мокирования

### Развертывание

- Поддержка разных окружений через `APP_ENV`
- Конфигурация через `.env` файлы
- Скрипты для запуска и переустановки окружения
- Готовность к контейнеризации (Docker)

---

## Заключение

Проект следует современным практикам разработки backend приложений:
- Четкая структура каталогов
- Разделение ответственности
- Использование проверенных технологий
- Готовность к масштабированию
- Безопасность и валидация данных
- Автоматическая документация API

Архитектура позволяет легко добавлять новые функции, тестировать код и поддерживать проект в долгосрочной перспективе.

