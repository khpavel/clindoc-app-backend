# Структура проекта и подход к разработке

## Общее описание проекта

**CSR Assistant Backend** — это backend API для приложения CSR Assistant, построенный на FastAPI. Проект предназначен для управления клиническими исследованиями (studies), создания и редактирования документов CSR (Clinical Study Report), работы с исходными документами и интеграции с AI для генерации текста разделов.

---

## Структура каталогов

### Корневой каталог проекта

```
clindoc-app-backend/
├── app/                    # Основное приложение
├── alembic/               # Миграции базы данных
├── storage/               # Хранилище загруженных файлов
├── venv/                  # Виртуальное окружение Python (не коммитится)
├── alembic.ini            # Конфигурация Alembic
├── requirements.txt       # Зависимости Python
├── README.md              # Документация проекта
├── API_SPECIFICATION.md   # Спецификация API
├── run_server.bat         # Скрипт запуска сервера (Windows CMD)
├── run_server.ps1          # Скрипт запуска сервера (Windows PowerShell)
├── reinstall_venv.bat     # Скрипт переустановки venv (Windows CMD)
└── reinstall_venv.ps1     # Скрипт переустановки venv (Windows PowerShell)
```

---

## Детальное описание каталогов и файлов

### 📁 `app/` — Основное приложение

Главный каталог приложения, содержащий всю бизнес-логику, API endpoints, модели данных и сервисы.

#### 📄 `app/main.py`
**Назначение:** Точка входа приложения FastAPI.

**Основные функции:**
- Инициализация FastAPI приложения
- Настройка CORS middleware для работы с фронтендом
- Настройка CorrelationIdMiddleware для отслеживания запросов
- Регистрация всех API роутеров (auth, studies, csr, ai, sources, templates, rag, qc, documents)
- Глобальный обработчик исключений с добавлением correlation ID
- Создание таблиц базы данных при старте (для MVP)
- Настройка логирования с correlation ID
- Кастомная конфигурация OpenAPI схемы для Swagger UI
- Health check endpoint (`/health`)

**Ключевые компоненты:**
- Подключение всех моделей SQLAlchemy для их регистрации
- Настройка CORS для `localhost:5173` и `127.0.0.1:5173` (Vite dev server)
- Интеграция всех роутеров с префиксом `/api/v1`
- CorrelationIdMiddleware добавляется первым для установки correlation ID для всех запросов
- Глобальный обработчик HTTPException добавляет correlation ID в ответы
- Глобальный обработчик Exception логирует ошибки с correlation ID и возвращает безопасные сообщения (в production скрывает детали ошибок)
- Кастомная OpenAPI схема с улучшенным описанием для Swagger UI
- Логирование настраивается в зависимости от окружения (WARNING для prod, INFO для dev)

---

#### 📁 `app/api/` — API Endpoints

Каталог содержит все HTTP endpoints приложения, организованные по версиям API.

##### 📁 `app/api/v1/` — API версии 1

Содержит роутеры для всех endpoints версии 1 API.

###### 📄 `app/api/v1/auth.py`
**Назначение:** Endpoints для аутентификации пользователей.

**Endpoints:**
- `POST /api/v1/auth/register` — регистрация нового пользователя
- `POST /api/v1/auth/token` — получение JWT токена (OAuth2 password flow)
- `POST /api/v1/auth/logout` — выход из системы (статусный endpoint, токен удаляется на клиенте)

**Функциональность:**
- Проверка уникальности username и email
- Хеширование паролей с помощью bcrypt
- Генерация JWT токенов
- Валидация данных через Pydantic схемы

###### 📄 `app/api/v1/studies.py`
**Назначение:** Endpoints для управления клиническими исследованиями и участниками исследований.

**Endpoints:**
- `POST /api/v1/studies` — создание нового исследования
- `GET /api/v1/studies` — получение списка исследований, где текущий пользователь является участником
- `GET /api/v1/studies/{study_id}` — получение конкретного исследования по ID
- `GET /api/v1/studies/{study_id}/members` — получение списка участников исследования
- `GET /api/v1/studies/{study_id}/members/me` — получение информации о членстве текущего пользователя в исследовании
- `POST /api/v1/studies/{study_id}/members/{user_id}` — добавление участника к исследованию
- `DELETE /api/v1/studies/{study_id}/members/{member_id}` — удаление участника из исследования (примечание: параметр `member_id` в пути фактически является `user_id` участника, а не ID записи StudyMember)

**Функциональность:**
- CRUD операции для исследований
- Проверка уникальности кода исследования
- Управление участниками исследований (члены команды)
- При создании исследования автоматически создается запись StudyMember с ролью "owner" для создателя и устанавливается `owner_id`
- Список исследований фильтруется по членству пользователя (только исследования, где пользователь является участником)
- Управление участниками доступно только владельцам исследования (owner)
- Получение информации о собственном членстве в исследовании через endpoint `/members/me`
- Требует аутентификации (через `get_current_active_user`)

###### 📄 `app/api/v1/csr.py`
**Назначение:** Endpoints для работы с документами CSR (Clinical Study Report).

**Endpoints:**
- `GET /api/v1/csr/{study_id}` — получение CSR документа для исследования
- `GET /api/v1/csr/document/{document_id}` — получение CSR документа по ID документа (Document.id)
- `GET /api/v1/csr/{study_id}/sections` — получение всех разделов CSR
- `GET /api/v1/csr/sections/{section_id}/versions/latest` — получение последней версии раздела
- `POST /api/v1/csr/sections/{section_id}/versions` — создание новой версии раздела
- `POST /api/v1/csr/sections/{section_id}/apply-template` — применение шаблона к разделу CSR
- `GET /api/v1/csr/{study_id}/export/docx` — экспорт CSR документа в формат DOCX

**Функциональность:**
- Автоматическое создание CSR с дефолтными разделами при первом обращении
- Поддержка навигации по CSR через `study_id` или `document_id` (Document.id)
- Единообразная логика загрузки CSR через сервис `csr_document_link` для обоих типов endpoints
- Версионирование разделов CSR
- Управление порядком разделов через `order_index`
- Применение шаблонов к разделам с автоматическим рендерингом и созданием версии
- Экспорт CSR документа в формат DOCX с правильным форматированием (заголовки, параграфы)
- Проверка доступа к исследованию (через `get_study_for_user_or_403`, `verify_study_access` или `get_document_for_user_or_403`)
- Требует аутентификации для применения шаблонов и экспорта (через `get_current_active_user`)

###### 📄 `app/api/v1/ai.py`
**Назначение:** Endpoints для интеграции с AI/LLM для генерации текста.

**Endpoints:**
- `POST /api/v1/ai/generate-section-text` — генерация текста для раздела CSR

**Функциональность:**
- Интеграция с LLM для генерации текста разделов CSR
- Принимает `section_id` и `prompt` для генерации текста
- Поддержка двух режимов работы: `stub` (заглушка) и `real` (реальная интеграция с LLM)
- Режим работы определяется настройкой `AI_MODE` в конфигурации
- Автоматическое построение промпта через `ai_prompt_builder` или использование промпт-шаблонов
- Автоматическое извлечение RAG контекста из исходных документов (protocol, SAP, TLF, previous CSR)
- Использование текущего текста раздела (если существует) для улучшения генерации
- Возвращает сгенерированный текст и информацию о модели
- Логирование всех вызовов AI в таблицу `ai_call_logs` с указанием режима работы (`mode`)

###### 📄 `app/api/v1/sources.py`
**Назначение:** Endpoints для загрузки и управления исходными документами.

**Endpoints:**
- `POST /api/v1/sources/{study_id}/upload` — загрузка исходного документа
- `GET /api/v1/sources/{study_id}` — получение списка документов исследования
- `DELETE /api/v1/sources/{source_document_id}` — мягкое удаление исходного документа (архивирование)
- `DELETE /api/v1/sources/{source_document_id}/permanent` — постоянное удаление исходного документа
- `POST /api/v1/sources/{source_document_id}/restore` — восстановление архивированного документа

**Функциональность:**
- Загрузка файлов через `multipart/form-data`
- Параметры загрузки: `file` (обязательно), `type` (обязательно), `language` (по умолчанию "ru"), `version_label` (опционально)
- Сохранение файлов в `storage/study_{study_id}/`
- Хранение метаданных в базе данных
- Поддержка различных типов документов (protocol, sap, tlf, csr_prev)
- Поддержка языков: "ru" и "en"
- Управление версиями документов через `is_current` (при загрузке нового документа с тем же типом и языком, предыдущие помечаются как `is_current=False`)
- Автоматическая индексация загруженных документов в RAG систему (если `is_rag_enabled=True`)
- Отслеживание статуса индексации через `index_status` (not_indexed, indexed, error)
- Проверка доступа к исследованию (через `get_study_for_user_or_403`)
- Мягкое удаление (DELETE): устанавливает `status="archived"`, удаляет связанные RAG чанки, файл остается на диске (доступно для owner и editor)
- Постоянное удаление (DELETE /permanent): удаляет документ из БД, связанные RAG чанки и физический файл (доступно только для owner)
- Восстановление (POST /restore): возвращает документ в активное состояние и запускает повторную индексацию (доступно для owner и editor)
- Требует аутентификации (через `get_current_active_user`)

###### 📄 `app/api/v1/templates.py`
**Назначение:** Endpoints для работы с шаблонами документов.

**Endpoints:**
- `GET /api/v1/templates/section/{section_code}` — получение шаблонов для конкретного раздела
- `POST /api/v1/templates/{template_id}/render` — рендеринг шаблона с подстановкой данных

**Функциональность:**
- Получение шаблонов по коду раздела (section_code) с фильтрацией по языку и области видимости
- Рендеринг шаблонов с построением контекста из данных исследования
- Поддержка дополнительного контекста для рендеринга
- Возврат информации об использованных и отсутствующих переменных

###### 📄 `app/api/v1/rag.py`
**Назначение:** Endpoints для работы с RAG (Retrieval-Augmented Generation) системой.

**Endpoints:**
- `POST /api/v1/rag/ingest/{source_document_id}` — запуск индексации исходного документа в RAG
- `GET /api/v1/rag/{study_id}` — получение чанков RAG для исследования с фильтрацией и пагинацией

**Функциональность:**
- Индексация исходных документов: извлечение текста, разбиение на чанки, сохранение в БД
- Получение чанков с фильтрацией по типу источника (`source_type`) и поиском по тексту (`q`)
- Поиск по тексту использует `ilike` для case-insensitive substring поиска
- Пагинация результатов (limit/offset): `limit` по умолчанию 50, максимум 500; `offset` по умолчанию 0
- Возвращает общее количество чанков (`total_chunks`) для пагинации
- Сортировка результатов по `source_type` и `order_index`
- Возвращает `text_preview` (первые 200 символов) для удобного отображения в списках
- Возвращает `source_document_file_name` для связи чанка с исходным документом
- Требует аутентификации для получения чанков (через `get_current_active_user`)

###### 📄 `app/api/v1/qc.py`
**Назначение:** Endpoints для работы с системой контроля качества (Quality Control) документов CSR.

**Endpoints:**
- `POST /api/v1/qc/documents/{document_id}/run` — запуск QC проверки для документа CSR
- `GET /api/v1/qc/issues/{study_id}` — получение списка QC issues для исследования с фильтрацией по статусу и серьёзности
- `GET /api/v1/qc/studies/{study_id}/issues` — получение списка QC issues для исследования с фильтрацией по document_id, статусу и серьёзности

**Функциональность:**
- Запуск набора базовых правил QC на документ и создание issues
- Получение списка QC issues с фильтрацией:
  - По статусу: `open`, `resolved`, `wont_fix`
  - По серьёзности: `info`, `warning`, `error`
  - По document_id (только для endpoint `/studies/{study_id}/issues`) — фильтрация issues по конкретному документу CSR
- Проверка доступа к исследованию (через `get_study_for_user_or_403` или `verify_study_access`)
- Требует аутентификации (через `get_current_active_user`)

###### 📄 `app/api/v1/documents.py`
**Назначение:** Endpoints для работы с документами исследований.

**Endpoints:**
- `GET /api/v1/studies/{study_id}/documents` — получение списка документов исследования
- `POST /api/v1/studies/{study_id}/documents` — создание нового документа для исследования

**Функциональность:**
- Управление документами различных типов: `csr`, `protocol`, `ib`, `sap`, `tlf`
- Поддержка статусов документов: `draft`, `in_qc`, `ready_for_submission`, `archived`
- Автоматическое создание дефолтного CSR документа при запросе списка (если `ensure_default_csr=true`)
- Сортировка документов по типу и дате создания
- Создание документов доступно только для владельцев и редакторов (viewer не может создавать)
- Требует аутентификации (через `get_current_active_user`)
- Проверка доступа к исследованию (через `get_study_for_user_or_403` или `verify_study_editor_access`)

---

#### 📁 `app/core/` — Основные настройки и утилиты

Каталог содержит конфигурацию приложения и базовые утилиты.

###### 📄 `app/core/config.py`
**Назначение:** Управление конфигурацией приложения через переменные окружения.

**Функциональность:**
- Загрузка настроек из `.env.dev` или `.env.prod` в зависимости от `APP_ENV`
- Настройки базы данных (`database_url`)
- Настройки JWT аутентификации (`secret_key`, `algorithm`, `access_token_expire_minutes`)
- Настройки хранилища файлов (`storage_dir`)
- Настройки AI:
  - `ai_mode` — режим работы AI: `"stub"` или `"real"`, по умолчанию `"stub"`
  - `ai_endpoint` — HTTP endpoint URL для LLM API (обязательно при `ai_mode="real"`)
  - `ai_api_key` — API ключ для аутентификации (опционально, зависит от провайдера)
- Настройка окружения (`app_env` — `"dev"` или `"prod"`, по умолчанию `"dev"`)
- Валидация обязательных параметров при старте

**Особенности:**
- Использует `pydantic-settings` для валидации
- Поддержка разных окружений (dev/prod)
- Автоматическое определение файла конфигурации по переменной `APP_ENV`
- Предупреждение при использовании дефолтного `storage_dir` в production
- Файлы загружаются в порядке: `.env.dev`/`.env.prod`, затем `.env` (если существует, переопределяет предыдущие)

###### 📄 `app/core/security.py`
**Назначение:** Утилиты для безопасности и аутентификации.

**Функциональность:**
- `get_password_hash()` — хеширование паролей с помощью bcrypt
- `verify_password()` — проверка паролей
- `create_access_token()` — создание JWT токенов

**Особенности:**
- Обработка ограничения bcrypt на длину пароля (72 байта)
- Использование `python-jose` для работы с JWT
- Настройка времени жизни токенов через конфигурацию

###### 📄 `app/core/storage.py`
**Назначение:** Утилиты для работы с файловым хранилищем.

**Функциональность:**
- `get_storage_dir()` — получение базовой директории хранилища
- `build_study_source_path()` — построение пути для файла исследования
- `delete_file()` — удаление файла из хранилища по относительному пути

**Особенности:**
- Автоматическое создание директорий при необходимости
- Организация файлов по исследованиям (`study_{study_id}/`)
- `delete_file()` принимает относительный путь (например, "study_1/protocol.pdf") и возвращает True при успешном удалении или False, если файл не найден или произошла ошибка
- Логирование операций удаления файлов

###### 📄 `app/core/middleware.py`
**Назначение:** Middleware для отслеживания запросов через correlation ID.

**Функциональность:**
- `CorrelationIdMiddleware` — middleware для добавления correlation ID к каждому запросу
- `CorrelationIdFilter` — фильтр логирования для добавления correlation ID в логи

**Особенности:**
- Correlation ID генерируется автоматически, если не передан в заголовке `X-Correlation-ID`
- Correlation ID сохраняется в `request.state` для доступа в обработчиках
- Correlation ID сохраняется в contextvar для использования в логах
- Correlation ID добавляется в заголовки ответа `X-Correlation-ID`
- Позволяет отслеживать запросы через логи и ответы API

---

#### 📁 `app/db/` — Работа с базой данных

Каталог содержит настройки подключения к базе данных и сессий SQLAlchemy.

###### 📄 `app/db/session.py`
**Назначение:** Настройка подключения к базе данных и управление сессиями.

**Функциональность:**
- Создание engine SQLAlchemy
- Настройка `SessionLocal` для создания сессий
- `get_db()` — dependency для FastAPI, предоставляющая сессию БД
- Базовый класс `Base` для моделей SQLAlchemy

**Особенности:**
- Использует connection pooling
- Автоматическое закрытие сессий после использования
- Интеграция с настройками из `app/core/config.py`

---

#### 📁 `app/deps/` — Dependencies для FastAPI

Каталог содержит зависимости (dependencies) для использования в endpoints.

###### 📄 `app/deps/auth.py`
**Назначение:** Dependencies для аутентификации и авторизации.

**Функциональность:**
- `get_current_user()` — получение текущего пользователя из JWT токена
- `get_current_active_user()` — получение активного пользователя (проверка `is_active`)

**Особенности:**
- Использует OAuth2PasswordBearer для извлечения токена из заголовка
- Валидация JWT токена
- Проверка существования пользователя в БД
- Обработка ошибок аутентификации

###### 📄 `app/deps/study_access.py`
**Назначение:** Dependencies для проверки доступа к исследованиям.

**Функциональность:**
- `verify_study_access()` — проверка доступа пользователя к исследованию (через StudyMember)
- `get_study_for_user_or_403()` — dependency для проверки доступа к исследованию
- `verify_study_management_access()` — проверка прав управления исследованием (только owner)
- `get_study_for_management_or_403()` — dependency для проверки прав управления исследованием
- `verify_study_editor_access()` — проверка прав редактирования исследования (owner или editor, viewer не допускается)
- `get_study_for_editor_or_403()` — dependency для проверки прав редактирования исследования

**Особенности:**
- Проверяет наличие записи StudyMember для пользователя и исследования
- Возвращает 403 Forbidden, если пользователь не является участником исследования
- Возвращает 404 Not Found, если исследование не существует
- Используется для защиты endpoints, требующих доступа к конкретному исследованию
- `verify_study_editor_access()` используется для операций, требующих прав owner или editor (например, удаление исходных документов)
- `verify_study_management_access()` используется только для операций, требующих прав owner (например, постоянное удаление документов)

---

#### 📁 `app/models/` — SQLAlchemy модели

Каталог содержит модели данных для работы с базой данных.

###### 📄 `app/models/ai.py`
**Назначение:** Модель для логирования вызовов AI/LLM.

**Поля:**
- `id` — первичный ключ
- `study_id` — ID исследования (опционально)
- `section_id` — ID раздела CSR (опционально)
- `user_id` — ID пользователя (опционально)
- `prompt` — промпт, отправленный в AI
- `generated_text` — сгенерированный текст (опционально, может отсутствовать при ошибке)
- `model_name` — название модели AI (опционально)
- `mode` — режим работы AI (`"stub"` или `"real"`, опционально)
- `success` — флаг успешности вызова
- `error_message` — сообщение об ошибке (опционально)
- `created_at` — дата и время вызова

###### 📄 `app/models/user.py`
**Назначение:** Модель пользователя.

**Поля:**
- `id` — первичный ключ
- `username` — уникальное имя пользователя
- `full_name` — полное имя
- `email` — email (опционально)
- `hashed_password` — хешированный пароль
- `is_active` — флаг активности пользователя
- `created_at` — дата создания

###### 📄 `app/models/study.py`
**Назначение:** Модель клинического исследования.

**Поля:**
- `id` — первичный ключ
- `code` — уникальный код исследования
- `title` — название исследования
- `phase` — фаза исследования (опционально)
- `owner_id` — ID владельца исследования (внешний ключ к users, опционально)
- `status` — статус исследования (enum: `draft`, `ongoing`, `closed`, `archived`, по умолчанию `"draft"`)
- `indication` — показание к применению (опционально)
- `sponsor_name` — название спонсора (опционально)

**Связи:**
- Study → User (многие к одному через owner_id, опционально)
- Study → CSRDocument (один к одному)
- Study → SourceDocument (один ко многим)
- Study → StudyMember (один ко многим)
- Study → Document (один ко многим)

###### 📄 `app/models/csr.py`
**Назначение:** Модели для CSR документов и их разделов.

**Модели:**
- `CSRDocument` — документ CSR, связанный с исследованием
- `CSRSection` — раздел CSR документа
- `CSRSectionVersion` — версия раздела с текстом и метаданными

**Поля CSRDocument:**
- `id` — первичный ключ
- `study_id` — ID исследования (уникальный)
- `title` — название документа
- `status` — статус документа (например, `draft`)
- `document_id` — ID связанного документа из таблицы `documents` (опционально, для связи с Document моделью)

**Поля CSRSection:**
- `id` — первичный ключ
- `document_id` — ID документа CSR
- `code` — код раздела (например, `SYNOPSIS`, `EFFICACY_PRIMARY`)
- `title` — название раздела
- `order_index` — порядковый номер для сортировки

**Поля CSRSectionVersion:**
- `id` — первичный ключ
- `section_id` — ID раздела
- `text` — текст версии раздела
- `created_at` — дата создания версии
- `created_by` — автор создания (опционально)
- `source` — источник версии (`human`, `ai`, `template`)
- `template_id` — ID использованного шаблона (опционально)

**Связи:**
- CSRDocument → Study (один к одному)
- CSRDocument → Document (многие к одному, опционально, через `document_id`)
- CSRSection → CSRDocument (многие к одному)
- CSRSectionVersion → CSRSection (многие к одному)
- CSRSectionVersion → Template (многие к одному, опционально)

###### 📄 `app/models/document.py`
**Назначение:** Модель документа исследования.

**Поля:**
- `id` — первичный ключ
- `study_id` — ID исследования (внешний ключ к studies)
- `type` — тип документа (`csr`, `protocol`, `ib`, `sap`, `tlf`)
- `title` — название документа
- `template_code` — код шаблона (опционально)
- `status` — статус документа (`draft`, `in_qc`, `ready_for_submission`, `archived`, по умолчанию `"draft"`)
- `current_version_label` — метка текущей версии (опционально)
- `created_by` — ID пользователя, создавшего документ (опционально, внешний ключ к users)
- `created_at` — дата создания
- `updated_at` — дата обновления

**Связи:**
- Document → Study (многие к одному)
- Document → CsrDocument (один к одному, опционально, для документов типа "csr")

**Особенности:**
- Модель Document представляет общий документ исследования, который может быть различных типов
- Для документов типа "csr" может существовать связанный CsrDocument с детальной структурой разделов
- Позволяет единообразно управлять различными типами документов в рамках исследования
- Используется для навигации по документам через единый `document_id`

###### 📄 `app/models/source.py`
**Назначение:** Модель исходного документа.

**Поля:**
- `id` — первичный ключ
- `study_id` — ID исследования
- `type` — тип документа (например, `protocol`, `sap`, `tlf`, `csr_prev`)
- `file_name` — имя файла
- `storage_path` — относительный путь к файлу на диске или ключ S3
- `uploaded_at` — дата загрузки
- `uploaded_by` — пользователь, загрузивший файл (опционально)
- `language` — язык документа (`"ru"` или `"en"`, по умолчанию `"ru"`)
- `version_label` — метка версии документа (опционально)
- `status` — статус документа (`"active"`, `"superseded"`, `"archived"`, по умолчанию `"active"`)
- `is_current` — флаг текущей версии документа (по умолчанию `True`)
- `is_rag_enabled` — флаг включения индексации в RAG (по умолчанию `True`)
- `index_status` — статус индексации (`"not_indexed"`, `"indexed"`, `"error"`, по умолчанию `"not_indexed"`)

**Ограничения:**
- `language` может быть только `"ru"` или `"en"` (CheckConstraint)
- `status` может быть только `"active"`, `"superseded"` или `"archived"` (CheckConstraint)
- `index_status` может быть только `"not_indexed"`, `"indexed"` или `"error"` (CheckConstraint)

**Связи:**
- SourceDocument → Study (многие к одному)

###### 📄 `app/models/template.py`
**Назначение:** Модель шаблона документа.

**Поля:**
- `id` — первичный ключ
- `name` — название шаблона
- `description` — описание шаблона (опционально)
- `type` — тип шаблона (`section_text`, `prompt`)
- `section_code` — код раздела CSR (например, `SYNOPSIS`, `OBJECTIVES`, `DESIGN`, `DISPOSITION`, `EFFICACY_PRIMARY`, `SAFETY_OVERVIEW`, `SAFETY_SAE_DEATHS`, `DISCUSSION`)
- `language` — язык шаблона (`ru`, `en`)
- `scope` — область видимости (`global`, `sponsor`, `study`)
- `sponsor_id` — ID спонсора (для будущего использования)
- `study_id` — ID исследования (для будущего использования)
- `content` — содержимое шаблона с переменными в формате `{{variable_name}}`
- `variables` — метаданные о переменных в формате JSON (опционально)
- `is_default` — флаг шаблона по умолчанию
- `is_active` — флаг активности шаблона
- `version` — версия шаблона
- `created_at` — дата создания
- `updated_at` — дата обновления
- `created_by` — автор создания (опционально)

**Особенности:**
- Поддержка шаблонов для генерации текста разделов (`section_text`)
- Поддержка промптов для AI (`prompt`)
- Система версионирования шаблонов
- Поддержка многоязычности
- В базе данных присутствуют seed-данные для шаблонов ЕАЭС/РФ на русском языке

###### 📄 `app/models/study_member.py`
**Назначение:** Модель участника исследования (члена команды).

**Поля:**
- `id` — первичный ключ
- `user_id` — ID пользователя (внешний ключ к users)
- `study_id` — ID исследования (внешний ключ к studies)
- `role` — роль участника (`"owner"`, `"editor"`, `"viewer"`)
- `created_at` — дата добавления участника

**Связи:**
- StudyMember → User (многие к одному)
- StudyMember → Study (многие к одному)

**Особенности:**
- Уникальное ограничение на пару (study_id, user_id) — один пользователь может быть участником исследования только один раз
- Индексы для оптимизации запросов по `user_id` и `study_id`
- Роли: owner (владелец), editor (редактор), viewer (наблюдатель)
- При создании исследования автоматически создается StudyMember с ролью "owner" для создателя

**Использование ролей для контроля доступа:**
- **owner** — полный доступ: может управлять участниками исследования, удалять документы (включая постоянное удаление), восстанавливать документы
- **editor** — доступ к редактированию: может загружать документы, удалять документы (мягкое удаление), восстанавливать документы, но не может управлять участниками или выполнять постоянное удаление
- **viewer** — доступ только для чтения: может просматривать исследования, документы и разделы CSR, но не может загружать, удалять или редактировать контент

###### 📄 `app/models/rag.py`
**Назначение:** Модель для хранения чанков текста для RAG системы.

**Поля:**
- `id` — первичный ключ
- `study_id` — ID исследования
- `source_document_id` — ID исходного документа (опционально, внешний ключ к `source_documents`)
- `source_type` — тип источника (`protocol`, `sap`, `tlf`, `csr_prev`)
- `order_index` — порядковый номер чанка в документе
- `text` — текст чанка
- `created_at` — дата создания

**Связи:**
- RagChunk → SourceDocument (многие к одному, опционально)

**Особенности:**
- Индекс для оптимизации запросов по `study_id`, `source_type`, `order_index`
- Чанки создаются при индексации исходных документов
- Используются для контекста при генерации текста через AI

###### 📄 `app/models/qc.py`
**Назначение:** Модели для системы контроля качества (Quality Control) документов CSR.

**Модели:**
- `QCRule` — правило QC для проверки документов
- `QCIssue` — найденная проблема при проверке QC

**Поля QCRule:**
- `id` — первичный ключ
- `code` — уникальный код правила
- `name` — название правила
- `description` — описание правила (опционально)
- `severity` — серьёзность правила (`info`, `warning`, `error`)
- `is_active` — флаг активности правила

**Поля QCIssue:**
- `id` — первичный ключ
- `study_id` — ID исследования (внешний ключ к studies)
- `document_id` — ID документа CSR (опционально, внешний ключ к csr_documents)
- `section_id` — ID раздела CSR (опционально, внешний ключ к csr_sections)
- `rule_id` — ID правила QC (внешний ключ к qc_rules)
- `severity` — серьёзность issue (`info`, `warning`, `error`)
- `status` — статус issue (`open`, `resolved`, `wont_fix`, по умолчанию `"open"`)
- `message` — сообщение о проблеме
- `created_at` — дата создания
- `resolved_at` — дата разрешения (опционально)
- `resolved_by` — пользователь, разрешивший issue (опционально)

**Связи:**
- QCIssue → Study (многие к одному)
- QCIssue → CsrDocument (многие к одному, опционально)
- QCIssue → CsrSection (многие к одному, опционально)
- QCIssue → QCRule (многие к одному)
- QCRule → QCIssue (один ко многим)

**Ограничения:**
- `severity` может быть только `"info"`, `"warning"` или `"error"` (CheckConstraint)
- `status` может быть только `"open"`, `"resolved"` или `"wont_fix"` (CheckConstraint)

**Особенности:**
- Система правил позволяет создавать переиспользуемые проверки качества
- Issues привязываются к исследованию, документу и/или разделу для точной локализации проблем
- Поддержка статусов позволяет отслеживать жизненный цикл найденных проблем

---

#### 📁 `app/schemas/` — Pydantic схемы

Каталог содержит Pydantic схемы для валидации входных и выходных данных API.

###### 📄 `app/schemas/user.py`
**Назначение:** Схемы для работы с пользователями.

**Схемы:**
- `UserCreate` — данные для создания пользователя
- `UserRead` — данные пользователя для ответа API
- `Token` — JWT токен и его тип
- `TokenData` — данные из JWT токена


###### 📄 `app/schemas/csr.py`
**Назначение:** Схемы для работы с CSR документами.

**Схемы:**
- `CSRDocumentRead` — данные CSR документа
- `CSRSectionRead` — данные раздела CSR
- `CSRSectionVersionRead` — данные версии раздела
- `CSRSectionVersionCreate` — данные для создания версии

###### 📄 `app/schemas/source.py`
**Назначение:** Схемы для работы с исходными документами.

**Схемы:**
- `SourceDocumentBase` — базовая схема документа
- `SourceDocumentCreate` — данные для создания документа (study_id, type, file_name, uploaded_by, language, version_label, status, is_rag_enabled)
- `SourceDocumentRead` — данные документа для ответа API (включает все поля модели, включая language, version_label, status, is_current, is_rag_enabled, index_status)

###### 📄 `app/schemas/ai.py`
**Назначение:** Схемы для работы с AI endpoints.

**Схемы:**
- `GenerateSectionTextRequest` — запрос на генерацию текста
- `GenerateSectionTextResponse` — ответ с сгенерированным текстом

###### 📄 `app/schemas/template.py`
**Назначение:** Схемы для работы с шаблонами.

**Схемы:**
- `TemplateBase` — базовая схема шаблона
- `TemplateRead` — данные шаблона для ответа API (включает полное содержимое и метаданные)
- `TemplateRenderRequest` — запрос на рендеринг шаблона (study_id, section_id, extra_context)
- `TemplateRenderResponse` — ответ с отрендеренным текстом и информацией о переменных

###### 📄 `app/schemas/study.py`
**Назначение:** Схемы для работы с исследованиями.

**Схемы:**
- `StudyBase` — базовая схема исследования (code, title, phase, status, indication, sponsor_name)
- `StudyCreate` — данные для создания исследования
- `StudyRead` — данные исследования для ответа API (включает id)

**Особенности:**
- Поддержка полей: `status`, `indication`, `sponsor_name`
- Статус использует enum `StudyStatus` со значениями: `draft`, `ongoing`, `closed`, `archived`
- Статус по умолчанию: `"draft"`

###### 📄 `app/schemas/study_member.py`
**Назначение:** Схемы для работы с участниками исследований.

**Схемы:**
- `StudyMemberRole` — enum ролей участника (`OWNER`, `EDITOR`, `VIEWER`)
- `StudyMemberAddPayload` — данные для добавления участника (role)
- `StudyMemberRead` — данные участника для ответа API (id, study_id, user_id, role, created_at, user)
- `StudyMemberMeResponse` — данные о членстве текущего пользователя в исследовании (аналогично StudyMemberRead)

**Особенности:**
- Поддержка ролей: owner, editor, viewer
- Опциональное включение данных пользователя в ответ
- Отдельная схема для получения собственного членства

###### 📄 `app/schemas/document.py`
**Назначение:** Схемы для работы с документами исследований.

**Схемы:**
- `DocumentBase` — базовая схема документа (type, title, template_code, status, current_version_label)
- `DocumentCreate` — данные для создания документа
- `DocumentRead` — данные документа для ответа API (включает id, study_id, created_by, created_at, updated_at)

**Особенности:**
- Валидация типов документов: `csr`, `protocol`, `ib`, `sap`, `tlf`
- Валидация статусов: `draft`, `in_qc`, `ready_for_submission`, `archived`
- Статус по умолчанию: `"draft"`

###### 📄 `app/schemas/rag.py`
**Назначение:** Схемы для работы с RAG системой.

**Схемы:**
- `RagChunkRead` — данные чанка для ответа API (id, study_id, source_document_id, source_type, order_index, text, text_preview, source_document_file_name, created_at)
- `RagStudyChunksResponse` — ответ со списком чанков для исследования (study_id, source_type, total_chunks, limit, offset, chunks)

**Особенности:**
- Поддержка фильтрации по `source_type` и поиска по тексту через параметр `q`
- Возвращает общее количество чанков (`total_chunks`) для реализации пагинации на клиенте
- `text_preview` содержит усеченную версию текста (первые 200 символов) для отображения в списках
- `source_document_file_name` содержит имя файла исходного документа для удобства

###### 📄 `app/schemas/qc.py`
**Назначение:** Схемы для работы с системой контроля качества.

**Схемы:**
- `QCRuleBase` — базовая схема правила QC (code, name, description, severity, is_active)
- `QCRuleRead` — данные правила для ответа API (включает id)
- `QCIssueBase` — базовая схема issue QC (study_id, document_id, section_id, rule_id, severity, status, message)
- `QCIssueRead` — данные issue для ответа API (включает id, created_at, resolved_at, resolved_by)
- `QCIssueListResponse` — ответ со списком issues (issues, total)
- `QCRunResponse` — ответ на запуск QC проверки (document_id, issues_created, message)

**Особенности:**
- Поддержка статусов: `open`, `resolved`, `wont_fix`
- Поддержка уровней серьёзности: `info`, `warning`, `error`
- Отслеживание разрешения issues через `resolved_at` и `resolved_by`

---

#### 📁 `app/services/` — Бизнес-логика и сервисы

Каталог содержит бизнес-логику, вынесенную из endpoints для переиспользования.

###### 📄 `app/services/csr_defaults.py`
**Назначение:** Сервис для создания CSR документов с дефолтными разделами.

**Функциональность:**
- Создание CSR документа с предопределенными разделами
- Настройка порядка разделов через `order_index`
- Инициализация структуры CSR при первом обращении

###### 📄 `app/services/template_renderer.py`
**Назначение:** Сервис для рендеринга шаблонов документов.

**Функциональность:**
- `render_template_string()` — замена плейсхолдеров `{{variable}}` в тексте шаблона значениями из контекста
- `render_template_content()` — обёртка для работы с объектами Template
- Возвращает отрендеренный текст, список использованных переменных и список отсутствующих переменных
- Отсутствующие переменные остаются в тексте как плейсхолдеры

###### 📄 `app/services/template_context.py`
**Назначение:** Сервис для построения контекста для рендеринга шаблонов.

**Функциональность:**
- `build_template_context()` — построение базового контекста из данных исследования
- Загружает данные исследования из БД
- Создаёт словарь с переменными: `study_id`, `study_code`, `study_title`, `phase`, `indication`, `sponsor_name`
- `add_rag_context_to_template_context()` — добавление контекста из RAG системы
- Автоматически добавляет RAG контекст из исходных документов: `context_protocol`, `context_sap`, `context_tlf_summary`, `context_csr_prev`
- Поддерживает дополнительный контекст через параметр `extra_context`

###### 📄 `app/services/text_extraction.py`
**Назначение:** Сервис для извлечения текста из различных форматов файлов.

**Функциональность:**
- `extract_text_from_file()` — извлечение текста из файла
- Поддержка форматов: `.txt`, `.md`, `.pdf`, `.docx`, `.doc`, `.xlsx`, `.xls`
- Для PDF использует `pypdf`
- Для Word документов использует `python-docx`
- Для Excel использует `openpyxl` и `xlrd` (для старых форматов)
- Обработка ошибок и сообщения о неподдерживаемых форматах

###### 📄 `app/services/rag_chunking.py`
**Назначение:** Сервис для разбиения текста на чанки для RAG системы.

**Функциональность:**
- `chunk_text()` — разбиение текста на чанки оптимального размера
- Разбиение по двойным переносам строк (параграфы)
- Настраиваемые параметры: `max_chunk_size` (по умолчанию 1000), `min_chunk_size` (по умолчанию 300)
- Объединение маленьких чанков для достижения минимального размера
- Очистка лишних пробелов и переносов строк

###### 📄 `app/services/rag_ingest.py`
**Назначение:** Сервис для индексации исходных документов в RAG систему.

**Функциональность:**
- `ingest_source_document_to_rag()` — полный процесс индексации документа
- Загрузка исходного документа из БД
- Извлечение текста через `extract_text_from_file()`
- Разбиение на чанки через `chunk_text()`
- Удаление старых чанков для документа (если есть)
- Создание записей `RagChunk` в БД
- Возвращает количество созданных чанков

###### 📄 `app/services/rag_retrieval.py`
**Назначение:** Сервис для извлечения релевантных чанков из RAG системы.

**Функциональность:**
- `retrieve_rag_chunks()` — получение чанков по типам источников
- Фильтрация по `study_id` и `source_type`
- Ограничение количества чанков на тип источника (`limit_per_source`)
- Возвращает словарь `{source_type: [RagChunk, ...]}`
- `build_rag_context_text()` — построение текстового контекста из чанков
- Объединение текстов чанков в строки для каждого типа источника

###### 📄 `app/services/study_members.py`
**Назначение:** Сервис для работы с участниками исследований.

**Функциональность:**
- `get_user_study_role()` — получение роли пользователя в исследовании
- Принимает `user_id` и `study_id`
- Возвращает роль (`"owner"`, `"editor"`, `"viewer"`) или `None`, если пользователь не является участником
- Используется для проверки прав доступа

###### 📄 `app/services/ai_client.py`
**Назначение:** Сервис для интеграции с AI/LLM.

**Функциональность:**
- `generate_section_text_stub()` — заглушка для генерации текста (для разработки и тестирования)
- `generate_section_text_real()` — реальная интеграция с внешним LLM через HTTP API
- Обе функции принимают параметры: `study_id`, `section_id`, `prompt`, `max_tokens`, `temperature`
- Обе функции возвращают кортеж `(generated_text, model_name)`
- Реальная интеграция использует переменные окружения: `AI_ENDPOINT` (обязательно) и `AI_API_KEY` (опционально)
- Режим работы выбирается через настройку `AI_MODE` в конфигурации (`"stub"` или `"real"`)

###### 📄 `app/services/ai_prompt_builder.py`
**Назначение:** Сервис для построения промптов для AI генерации текста разделов CSR.

**Функциональность:**
- `build_generate_section_prompt()` — построение полного промпта для AI генерации
- Принимает объекты `Study` и `CsrSection`, текущий текст раздела, RAG контекст по типам источников, пользовательский промпт
- Формирует структурированный промпт с инструкциями, контекстом из исходных документов, текущим текстом раздела и дополнительными инструкциями
- Возвращает готовую строку промпта для отправки в AI/LLM
- Используется, когда не найден промпт-шаблон для раздела

###### 📄 `app/services/docx_export.py`
**Назначение:** Сервис для экспорта CSR документов в формат DOCX.

**Функциональность:**
- `export_csr_to_docx()` — экспорт CSR документа в DOCX формат
- Принимает `document_id` и сессию БД
- Загружает CSR документ и все его разделы с последними версиями
- Создает DOCX документ с правильным форматированием:
  - Название документа как заголовок уровня 0 (центрированный)
  - Названия разделов как заголовки уровня 1
  - Текст разделов как параграфы
- Возвращает содержимое DOCX файла в виде байтов
- Использует библиотеку `python-docx` для создания документов

###### 📄 `app/services/csr_document_link.py`
**Назначение:** Сервис для связи CSR документов с моделью Document.

**Функциональность:**
- `get_or_create_csr_for_study()` — получение или создание CSRDocument для исследования через поиск/создание Document
- `get_or_create_csr_for_document()` — получение или создание CSRDocument, связанного с Document
- Обеспечивает единообразное поведение между study-based и document-based endpoints
- Автоматически создает Document типа "csr" при необходимости
- Автоматически связывает существующие CSRDocument с Document при миграции
- Создает дефолтные разделы CSR при создании нового CSRDocument

**Особенности:**
- Нормализованная точка входа для загрузки CSR документов
- Поддерживает обратную совместимость с существующими CSRDocument без связи с Document
- Используется в endpoints `GET /api/v1/csr/{study_id}` и `GET /api/v1/csr/document/{document_id}`

###### 📄 `app/services/qc_rules.py`
**Назначение:** Сервис для выполнения правил контроля качества документов CSR.

**Функциональность:**
- `get_or_create_required_sections_rule()` — получение или создание правила для проверки обязательных секций
- `check_required_sections()` — проверка наличия всех обязательных секций в документе
- `run_qc_rules_for_document()` — запуск всех активных QC правил для документа
- Проверка обязательных секций: сравнивает существующие секции документа с дефолтным набором секций из `DEFAULT_CSR_SECTIONS`
- Создает QCIssue для каждой отсутствующей обязательной секции
- Удаляет старые открытые issues для правила перед созданием новых (для избежания дублирования)
- Возвращает список всех созданных issues

**Особенности:**
- Расширяемая архитектура: легко добавлять новые правила QC
- Правила могут быть активными или неактивными через флаг `is_active`
- Каждое правило имеет уровень серьёзности (info, warning, error)

---

### 📁 `alembic/` — Миграции базы данных

Каталог содержит конфигурацию и скрипты миграций базы данных Alembic.

#### 📄 `alembic/env.py`
**Назначение:** Конфигурация окружения Alembic для выполнения миграций.

**Функциональность:**
- Настройка подключения к БД из `app.core.config`
- Регистрация всех моделей для автогенерации миграций
- Поддержка online и offline режимов миграций

#### 📄 `alembic/script.py.mako`
**Назначение:** Шаблон для генерации новых миграций.

#### 📄 `alembic/__init__.py`
**Назначение:** Инициализация пакета Alembic.

#### 📁 `alembic/versions/`
**Назначение:** Каталог для хранения файлов миграций.

**Содержимое:**
- Файлы миграций в формате `{revision}_{description}.py`
- Каждая миграция содержит изменения схемы БД (создание/изменение таблиц, колонок и т.д.)

**Последние миграции:**
- `xyz789ghi012_add_document_id_to_csr_documents.py` — добавление поля `document_id` в таблицу `csr_documents` для связи с моделью Document
- `abc123def456_add_documents_table.py` — добавление таблицы `documents` для управления документами исследований
- `1ac823d3cacc_merge_qc_models_and_ai_logs_mode.py` — объединение миграций для QC моделей и поля mode в ai_call_logs
- `f7d17a798792_add_qc_models.py` — добавление моделей для системы контроля качества (QC)
- `fbdd055efa7e_add_fields_to_source_document.py` — добавление полей в модель SourceDocument (language, version_label, status, is_current, is_rag_enabled, index_status)
- `a1b2c3d4e5f6_add_unique_constraint_study_members.py` — добавление уникального ограничения на пару (study_id, user_id) в таблице study_members
- `b6f382ad0f4f_add_study_members_and_owner_id.py` — добавление модели StudyMember и поля owner_id в модель Study
- `0f5493d5032a_add_study_status_indication_sponsor.py` — добавление полей status, indication, sponsor_name в модель Study
- `3a6c93eefa06_seed_eaes_ru_section_templates.py` — добавление seed-данных для шаблонов текста разделов ЕАЭС/РФ на русском языке (8 разделов: SYNOPSIS, OBJECTIVES, DESIGN, DISPOSITION, EFFICACY_PRIMARY, SAFETY_OVERVIEW, SAFETY_SAE_DEATHS, DISCUSSION)
- `d0ef26856a43_seed_eaes_ru_prompt_templates.py` — добавление seed-данных для промпт-шаблонов ЕАЭС/РФ на русском языке (8 промптов для AI-генерации текста разделов)
- `4fd91a5a7c9c_add_template_id_to_csr_section_versions.py` — добавление поля `template_id` в таблицу `csr_section_versions` для связи версий разделов с использованными шаблонами
- `add_mode_to_ai_call_logs.py` — добавление поля `mode` в таблицу `ai_call_logs` для отслеживания режима работы AI (stub/real)

---

### 📁 `storage/` — Файловое хранилище

**Назначение:** Хранилище загруженных исходных документов.

**Структура:**
```
storage/
└── study_{study_id}/
    ├── file1.pdf
    ├── file2.pdf
    └── ...
```

**Особенности:**
- Файлы организованы по исследованиям
- Каждое исследование имеет свою директорию
- Файлы сохраняются с оригинальными именами

---

### 📁 `venv/` — Виртуальное окружение

**Назначение:** Виртуальное окружение Python с установленными зависимостями.

**Примечание:** Этот каталог не должен коммититься в Git (должен быть в `.gitignore`).

---

## Файлы в корне проекта

### 📄 `alembic.ini`
**Назначение:** Конфигурационный файл Alembic для управления миграциями.

**Содержит:**
- Путь к скриптам миграций
- Настройки логирования
- Шаблоны для генерации миграций
- Конфигурация подключения к БД (переопределяется в `env.py`)

### 📄 `requirements.txt`
**Назначение:** Список зависимостей Python проекта.

**Основные зависимости:**
- `fastapi` — веб-фреймворк
- `uvicorn[standard]` — ASGI сервер
- `sqlalchemy` — ORM для работы с БД
- `psycopg2-binary` — драйвер PostgreSQL
- `pydantic-settings` — настройки из переменных окружения
- `python-dotenv` — загрузка `.env` файлов
- `alembic` — миграции БД
- `python-jose[cryptography]` — работа с JWT
- `bcrypt` — хеширование паролей
- `python-multipart` — обработка multipart/form-data
- `pytest`, `httpx` — тестирование
- `pypdf` — извлечение текста из PDF файлов
- `python-docx` — извлечение текста из Word документов (.docx) и создание DOCX файлов
- `openpyxl` — извлечение текста из Excel файлов (.xlsx)
- `xlrd` — извлечение текста из старых Excel файлов (.xls)

**Примечание:** Полный список зависимостей см. в файле `requirements.txt`

### 📄 `README.md`
**Назначение:** Основная документация проекта с инструкциями по установке и запуску.

### 📄 `API_SPECIFICATION.md`
**Назначение:** Детальная спецификация всех API endpoints с примерами запросов и ответов.

### 📄 `run_server.bat` / `run_server.ps1`
**Назначение:** Скрипты для запуска сервера разработки.

**Функциональность:**
- Активация виртуального окружения
- Запуск uvicorn с флагом `--reload` для автоматической перезагрузки

### 📄 `reinstall_venv.bat` / `reinstall_venv.ps1`
**Назначение:** Скрипты для переустановки виртуального окружения.

**Функциональность:**
- Удаление старого venv
- Создание нового venv
- Установка зависимостей из `requirements.txt`

---

## Подход к разработке

### Архитектурный подход

Проект использует **многослойную архитектуру (Layered Architecture)** с четким разделением ответственности:

1. **Слой представления (Presentation Layer)** — `app/api/v1/`
   - Обработка HTTP запросов и ответов
   - Валидация входных данных через Pydantic схемы
   - Минимальная бизнес-логика

2. **Слой бизнес-логики (Business Logic Layer)** — `app/services/`
   - Вынесенная бизнес-логика
   - Переиспользуемые сервисы
   - Обработка сложных операций

3. **Слой доступа к данным (Data Access Layer)** — `app/models/`, `app/db/`
   - SQLAlchemy модели
   - Управление сессиями БД
   - ORM маппинг

4. **Слой инфраструктуры (Infrastructure Layer)** — `app/core/`
   - Конфигурация
   - Утилиты безопасности
   - Файловое хранилище

### Принципы проектирования

#### 1. **Separation of Concerns (Разделение ответственности)**
- Каждый модуль отвечает за свою область
- API endpoints не содержат бизнес-логику
- Модели БД отделены от схем валидации

#### 2. **Dependency Injection (Внедрение зависимостей)**
- Использование FastAPI Depends для внедрения зависимостей
- `get_db()` для сессий БД
- `get_current_user()` для аутентификации
- Легкое тестирование через моки

#### 3. **Versioning API**
- API организовано по версиям (`/api/v1/`)
- Возможность добавления новых версий без breaking changes

#### 4. **Configuration Management**
- Настройки через переменные окружения
- Поддержка разных окружений (dev/prod)
- Валидация конфигурации при старте

#### 5. **Database Migrations**
- Использование Alembic для управления схемой БД
- Версионирование изменений БД
- Автогенерация миграций из моделей

### Паттерны проектирования

#### 1. **Repository Pattern (частично)**
- Модели SQLAlchemy используются напрямую в endpoints
- Для сложных случаев можно добавить репозитории

#### 2. **Service Layer Pattern**
- Бизнес-логика вынесена в `app/services/`
- Пример: `csr_defaults.py` для создания дефолтных CSR

#### 3. **Dependency Injection Pattern**
- Широкое использование FastAPI Depends
- Легкая замена зависимостей для тестирования

#### 4. **Factory Pattern**
- `SessionLocal` для создания сессий БД
- `get_storage_dir()` для создания путей к файлам

### Технологический стек

- **FastAPI** — современный, быстрый веб-фреймворк с автоматической документацией
- **SQLAlchemy** — мощный ORM для работы с БД
- **PostgreSQL** — реляционная БД
- **Pydantic** — валидация данных и настройки
- **Alembic** — миграции БД
- **JWT (python-jose)** — аутентификация через токены
- **bcrypt** — безопасное хеширование паролей

### Подход к безопасности

1. **Аутентификация:**
   - JWT токены с настраиваемым временем жизни
   - OAuth2 password flow
   - Хеширование паролей с помощью bcrypt

2. **Авторизация:**
   - Проверка активности пользователя
   - Dependency для проверки прав доступа

3. **Валидация данных:**
   - Pydantic схемы для валидации входных данных
   - Защита от SQL injection через SQLAlchemy
   - Проверка типов на уровне API

### Подход к разработке

1. **Итеративная разработка:**
   - MVP подход с базовой функциональностью
   - Постепенное добавление функций

2. **Автоматическая документация:**
   - Swagger UI (`/docs`)
   - ReDoc (`/redoc`)
   - OpenAPI схема (`/openapi.json`)

3. **Логирование:**
   - Настроенное логирование через стандартный модуль Python
   - Глобальный обработчик исключений с логированием

4. **CORS:**
   - Настроен для работы с фронтендом на `localhost:5173`
   - Гибкая настройка для разных окружений

### Управление версиями и миграциями

- **Alembic** для управления схемой БД
- Автогенерация миграций из моделей
- Версионирование изменений БД
- Поддержка отката миграций

### Тестирование

- Использование `pytest` для unit и integration тестов
- `httpx` для тестирования API endpoints
- Зависимости настроены для легкого мокирования

### Развертывание

- Поддержка разных окружений через `APP_ENV`
- Конфигурация через `.env` файлы
- Скрипты для запуска и переустановки окружения
- Готовность к контейнеризации (Docker)

---

## Заключение

Проект следует современным практикам разработки backend приложений:
- Четкая структура каталогов
- Разделение ответственности
- Использование проверенных технологий
- Готовность к масштабированию
- Безопасность и валидация данных
- Автоматическая документация API

Архитектура позволяет легко добавлять новые функции, тестировать код и поддерживать проект в долгосрочной перспективе.

